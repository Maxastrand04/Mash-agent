# 3.0 — Verification Test Plan for Phase 3

Headless verification of every implementation plan in Phase 3 of [mash-agent-plan.md](../mash-agent-plan.md): [3.1 bugs found V1–V4](../implementation_plans/3.1_bugs_found_and_fixes.md), [3.2 short-filename source selection](../implementation_plans/3.2_source_selection_bug.md), [3.3 shallowest path default](../implementation_plans/3.3_shallow_to_default.md), [3.4 improved menus](../implementation_plans/3.4_improved_menus.md), [3.5 bug fix](../implementation_plans/3.5_bug_fix.md), [3.6 touch bug fix](../implementation_plans/3.6_touch_bug_fix.md), [3.7 rename extension bug](../implementation_plans/3.7_rename_extension_bug.md), [3.8 mash behaviour update](../implementation_plans/3.8_mash_behaviour_update.md), [3.9 list/open/cat + create-without-marker](../implementation_plans/3.9_mash_behaviour_update.md), [3.10 list-flow fixes + move/copy missing-destination + bare-name extension](../implementation_plans/3.10_mash_behavior_update.md). Includes a full regression of Phase 2 ([2.3 bug fixes](../implementation_plans/2.3_bug_fixes.md)) and a re-run of the [2.1 stress test suite](2.1_stress_test.md). Every test runs inside `Claude_testing/`.

**Goal:** Confirm all Phase 3 work behaves as specified. Earlier phases (3.1–3.4) covered V1–V4 fixes, short-name source disambiguation, shallowest-match default, and the improved menu flows. The new phases (8–13) cover 3.5–3.10: rename extension reconciliation, touch destination, the universal menu template (absolute paths + `(Current directory)` suffix + single-match numbered), delete/open/cat confirmation prompts, list two-stage flow with `(folder)` annotations, list-routed rename keeping extension, list typed-prompt fuzzy match, the `mash create testing.pdf` destination bug, the move/copy `unknown` destination bug, the file/folder disambiguation for bare `mash create drafts`, and the bare-name extension prompt.

**Status legend:** ⬜ Not started · 🟡 In progress · ✅ Done

---

## Constants

```
MASH="/Users/max/Documents/GitHub/Mash agent/.venv/bin/mash"
DIR="/Users/max/Documents/GitHub/Mash agent/Claude_testing"
```

Every mash invocation is run from `$DIR`:

```
cd "$DIR" && "$MASH" <flags> <prompt>
```

For interactive menus without `--yes`/`--dry-run`, feed input via stdin:

```
cd "$DIR" && printf '1\n' | "$MASH" <prompt>
```

For multi-step interactive flows (e.g. retype → re-resolve → pick), chain inputs with `\n`:

```
cd "$DIR" && printf 'notes\n1\nn\n' | "$MASH" delete note
```

---

## Phase 0 — Fixture reset

Reset `Claude_testing/` to a clean, known state. **All operations stay inside `$DIR`.**

| # | Command | Status |
|---|---------|--------|
| 0.1 | `cd "$DIR" && find . -mindepth 1 -not -path "*/.*" -not -name "bugs_found.md" -delete` (preserves `bugs_found.md`) | ⬜ |
| 0.2 | `mkdir -p "$DIR/docs" "$DIR/reports" "$DIR/archive" "$DIR/tmp" "$DIR/src/deep/nested"` | ⬜ |
| 0.3 | `touch "$DIR/docs/overview.md" "$DIR/docs/notes.txt" "$DIR/archive/notes.txt" "$DIR/reports/summary.pdf" "$DIR/main.py" "$DIR/src/deep/nested/main.py" "$DIR/config.json" "$DIR/data.csv"` | ⬜ |
| 0.4 | `find "$DIR" -not -path "*/.*"` (verify tree) | ⬜ |

Two `notes.txt`, two `main.py` at different depths (root vs `src/deep/nested/`), a single `reports/` directory, plus assorted file types so the extension-submenu has real data to draw from.

---

## Phase 1 — Regression: Phase 2 (2.3) bug fixes

Confirm the B1/B2/B3 fixes still hold.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 1.1 | `cd "$DIR" && "$MASH" --dry-run list files in docs` | `Run: ls -la ./docs` (or equivalent). No `[debug] resolved:`. No source menu. | ⬜ |
| 1.2 | `cd "$DIR" && "$MASH" --dry-run rename main.py to app.py` | Source disambiguates between root and deep `main.py` (see Phase 3 for ordering). `Run: mv <chosen> ./app.py`. | ⬜ |
| 1.3 | `cd "$DIR" && printf '\n' \| "$MASH" delete notes.txt` | Menu lists both `notes.txt` paths. Blank input cancels; no `Run:`. | ⬜ |
| 1.4 | `cd "$DIR" && "$MASH" --dry-run move data.csv to the reports folder` | `Run: mv ./data.csv ./reports/` (directory, NOT `./reports/summary.pdf`). | ⬜ |
| 1.5 | `cd "$DIR" && "$MASH" --dry-run rename main.py to app.py 2>&1 \| grep -c "\[debug\]"` | `0` | ⬜ |

---

## Phase 2 — Verify 3.1 fixes (V1–V4: hints become post-processing)

Menu selections must be honoured even when the LLM ignores hints. Post-processing (`_apply_source`, `_apply_destination`, `_apply_filename`) is the contract.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 2.1 | `cd "$DIR" && printf '2\n' \| "$MASH" --dry-run delete notes.txt` | `Run:` line uses the second listed candidate exactly, regardless of what the LLM emitted. | ⬜ |
| 2.2 | `cd "$DIR" && printf '2\n' \| "$MASH" --dry-run move data.csv to reports` (with both `./reports` and `./tmp/reports` present — create `mkdir -p "$DIR/tmp/reports"` first) | `Run:` line ends with chosen destination `./tmp/reports/`. Cleanup `rm -rf "$DIR/tmp/reports"` after. | ⬜ |
| 2.3 | `cd "$DIR" && "$MASH" --dry-run create a python file called hello world` | Auto-selected filename is `hello_world.py` and appears in `Run:` line, even if LLM emitted a different form. | ⬜ |
| 2.4 | `cd "$DIR" && "$MASH" --dry-run create a python file called exports` | Single-token name → no format menu. Resolver does NOT fire on `exports` for create-form prompts (tightened scope). | ⬜ |

---

## Phase 3 — Verify 3.2 (short filenames trigger source disambiguation)

`_is_filename_candidate` is gone; every non-stop-word is tried against the tree. Short names like `main` must produce candidates.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 3.1 | `cd "$DIR" && printf '\n' \| "$MASH" remove main` | Menu appears listing BOTH `./main.py` and `./src/deep/nested/main.py`. Blank cancels. | ⬜ |
| 3.2 | `cd "$DIR" && printf '\n' \| "$MASH" move main into docs` | Source menu lists both `main.py` paths. Destination resolves to `./docs`. | ⬜ |
| 3.3 | `cd "$DIR" && "$MASH" --dry-run list files in docs` | Stop-word-only prompt still produces NO source menu (no false positives from new resolver path). | ⬜ |
| 3.4 | `cd "$DIR" && "$MASH" --dry-run delete xyz` | No fuzzy match found → no menu, LLM falls through to literal `./xyz`. | ⬜ |

---

## Phase 4 — Verify 3.3 (shallowest path is the default)

`resolve_paths` / `resolve_dirs` sort by path depth ascending.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 4.1 | `cd "$DIR" && printf '\n' \| "$MASH" remove main` | Candidate `1` in the menu is `./main.py` (root), NOT `./src/deep/nested/main.py`. | ⬜ |
| 4.2 | `cd "$DIR" && "$MASH" --dry-run remove main` | `[dry-run] Auto-selected: ./main.py`. `Run: rm ./main.py`. | ⬜ |
| 4.3 | `cd "$DIR" && "$MASH" --yes remove main` | Auto-selects `./main.py` (root) and deletes it. Verify `! test -f "$DIR/main.py"` and `test -f "$DIR/src/deep/nested/main.py"`. Restore: `touch "$DIR/main.py"`. | ⬜ |
| 4.4 | `mkdir -p "$DIR/a/reports"` then `cd "$DIR" && "$MASH" --dry-run move data.csv to reports` | Destination menu lists `./reports` BEFORE `./a/reports`. Auto-select picks `./reports`. Cleanup `rm -rf "$DIR/a"`. | ⬜ |

---

## Phase 5 — Verify 3.4 (improved menus)

### 5a — Source menu: type-to-retry and single-match confirm

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 5a.1 (single-match: blank confirms) | `cd "$DIR" && printf '\nn\n' \| "$MASH" delete overview` | Prompt: `Found: ./docs/overview.md. Press Enter to confirm, N to cancel, or type a different name:`. Blank confirms. `[y/N]` answered `n` → `Cancelled.`. | ⬜ |
| 5a.2 (single-match: N cancels) | `cd "$DIR" && printf 'N\n' \| "$MASH" delete overview` | Program prints cancellation and exits without `Run:`. | ⬜ |
| 5a.3 (single-match: retype) | `cd "$DIR" && printf 'config\n\nn\n' \| "$MASH" delete overview` | Retype `config` → re-resolves → single-match `./config.json` confirmed by blank → `[y/N]` answered `n`. | ⬜ |
| 5a.4 (multi-match: retype) | `cd "$DIR" && printf 'config\n\nn\n' \| "$MASH" delete notes.txt` | Multi-match menu for `notes.txt` shown; typed `config` re-resolves to single match `./config.json`; blank confirms. | ⬜ |
| 5a.5 (no-match: retype to find) | `cd "$DIR" && printf 'overview\n\nn\n' \| "$MASH" delete ghost` | Prompt: `No file found. Type a filename to search (Enter to cancel):`. Typed `overview` re-resolves to `./docs/overview.md`. | ⬜ |
| 5a.6 (no-match: blank cancels) | `cd "$DIR" && printf '\n' \| "$MASH" delete ghost` | Blank cancels at the no-match prompt. No `Run:`. | ⬜ |

### 5b — Destination menu: current dir always last, retype, no-match default

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 5b.1 (current dir included) | `cd "$DIR" && printf '\n' \| "$MASH" --dry-run move data.csv to reports` | Destination menu lists `./reports` then `.` as the **last** entry. Blank cancels. | ⬜ |
| 5b.2 (pick current dir) | `cd "$DIR" && printf '2\n' \| "$MASH" --dry-run move data.csv to reports` | Choosing `2` (current dir) produces `Run: mv ./data.csv ./`. | ⬜ |
| 5b.3 (destination retype) | `cd "$DIR" && printf 'archive\n1\n' \| "$MASH" --dry-run move data.csv to reports` | Typed `archive` re-resolves; new menu lists `./archive` then `.`; pick `1` → `Run: mv ./data.csv ./archive/`. | ⬜ |
| 5b.4 (no-match: blank accepts current) | `cd "$DIR" && printf '\n' \| "$MASH" --dry-run move data.csv to nowheredir` | Prompt: `No folder found, defaulting to current folder. Press Enter to confirm, type a name to search, or N to cancel:`. Blank → `Run: mv ./data.csv ./`. | ⬜ |
| 5b.5 (no-match: N cancels) | `cd "$DIR" && printf 'N\n' \| "$MASH" --dry-run move data.csv to nowheredir` | Cancelled, no `Run:`. | ⬜ |
| 5b.6 (no-match: retype) | `cd "$DIR" && printf 'archive\n1\n' \| "$MASH" --dry-run move data.csv to nowheredir` | Retype `archive` → re-resolves → pick `1` → `Run: mv ./data.csv ./archive/`. | ⬜ |

### 5c — Filename menu: pick / manual / extension submenu

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 5c.1 (numeric pick) | `cd "$DIR" && printf '2\n' \| "$MASH" --dry-run create a python file called hello world` | Selects format `2` (`hello-world.py`). `Run: touch ./hello-world.py`. | ⬜ |
| 5c.2 (blank cancels) | `cd "$DIR" && printf '\n' \| "$MASH" --dry-run create a python file called hello world` | Cancelled, no `Run:`. | ⬜ |
| 5c.3 (manual with extension) | `cd "$DIR" && printf 'my_custom.py\n' \| "$MASH" --dry-run create a python file called hello world` | `Run: touch ./my_custom.py`. | ⬜ |
| 5c.4 (manual no extension, known from prompt) | `cd "$DIR" && printf 'mycustom\n' \| "$MASH" --dry-run create a python file called hello world` | `python` in prompt → extension known. `Run: touch ./mycustom.py`. | ⬜ |
| 5c.5 (manual no extension, unknown → extension submenu, numeric pick) | `cd "$DIR" && printf 'mycustom\n1\n' \| "$MASH" --dry-run create a file called hello world` | Extension submenu lists ext present in tree (e.g. `md`, `txt`, `pdf`, `py`, `json`, `csv`) ordered by frequency. Pick `1` → that extension appended. | ⬜ |
| 5c.6 (extension submenu, manual with dot) | `cd "$DIR" && printf 'mycustom\n.md\n' \| "$MASH" --dry-run create a file called hello world` | Accepts `.md`, normalises, → `Run: touch ./mycustom.md`. | ⬜ |
| 5c.7 (extension submenu, manual without dot) | `cd "$DIR" && printf 'mycustom\nmd\n' \| "$MASH" --dry-run create a file called hello world` | Accepts `md`, → `Run: touch ./mycustom.md`. | ⬜ |
| 5c.8 (extension submenu, invalid → re-prompt) | `cd "$DIR" && printf 'mycustom\nxyzzy\nmd\n' \| "$MASH" --dry-run create a file called hello world` | Prints `Unknown extension 'xyzzy', try again.`; second attempt `md` succeeds. `Run: touch ./mycustom.md`. | ⬜ |
| 5c.9 (extension submenu, blank cancels) | `cd "$DIR" && printf 'mycustom\n\n' \| "$MASH" --dry-run create a file called hello world` | Cancelled, no `Run:`. | ⬜ |

### 5d — `--yes` / `--dry-run` auto-select unchanged

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 5d.1 | `cd "$DIR" && "$MASH" --dry-run remove main` | Auto-selects shallowest `./main.py` without any retype prompts. | ⬜ |
| 5d.2 | `cd "$DIR" && "$MASH" --yes create a python file called hello world` | Auto-picks format 1 → `Run: touch ./hello_world.py`. No menu interaction. | ⬜ |
| 5d.3 | `cd "$DIR" && "$MASH" --dry-run move data.csv to nowheredir` | Destination no-match under `--dry-run` → defaults to `.` without prompting. `Run: mv ./data.csv ./`. | ⬜ |

---

## Phase 6 — Regression: re-run the 2.1 stress test suite

Re-run every test from `2.1_stress_test.md` to confirm Phase 3 introduced no regressions.

### 6a — Phase 1 (dry-run correctness)

| # | Command | Expected `Run:` line | Status |
|---|---------|---------------------|--------|
| 6a.1 | `cd "$DIR" && "$MASH" --dry-run move data.csv to the reports folder` | `mv ./data.csv ./reports/` | ⬜ |
| 6a.2 | `cd "$DIR" && "$MASH" --dry-run rename main.py to app.py` | `mv <shallowest match> ./app.py` (auto-selects `./main.py`) | ⬜ |
| 6a.3 | `cd "$DIR" && "$MASH" --dry-run copy config.json to the archive folder` | `cp ./config.json ./archive/` | ⬜ |
| 6a.4 | `cd "$DIR" && "$MASH" --dry-run delete notes.txt` | `rm ./docs/notes.txt` or `rm ./archive/notes.txt` (after auto-select) | ⬜ |
| 6a.5 | `cd "$DIR" && "$MASH" --dry-run list files in docs` | `ls -la ./docs` | ⬜ |
| 6a.6 | `cd "$DIR" && "$MASH" --dry-run create a new directory called exports` | `mkdir -p ./exports` or `mkdir ./exports` | ⬜ |
| 6a.7 | `cd "$DIR" && "$MASH" --dry-run create a new file called readme.txt` | `touch ./readme.txt` | ⬜ |

### 6b — Phase 2 (live execution, requires `--yes`)

Re-set fixtures before this phase: `cd "$DIR" && find . -mindepth 1 -not -path "*/.*" -not -name "bugs_found.md" -delete && mkdir -p docs reports archive tmp src/deep/nested && touch docs/overview.md docs/notes.txt archive/notes.txt reports/summary.pdf main.py src/deep/nested/main.py config.json data.csv` (preserves `bugs_found.md`)

| # | Command | Verify | Status |
|---|---------|--------|--------|
| 6b.1 | `cd "$DIR" && "$MASH" --yes create a directory called sandbox` | `test -d "$DIR/sandbox"` succeeds | ⬜ |
| 6b.2 | `cd "$DIR" && "$MASH" --yes create a file called hello.txt inside sandbox` | `test -f "$DIR/sandbox/hello.txt"` succeeds | ⬜ |
| 6b.3 | `cd "$DIR" && "$MASH" --yes move hello.txt from sandbox to archive` | `test -f "$DIR/archive/hello.txt" && ! test -f "$DIR/sandbox/hello.txt"` | ⬜ |

### 6c — Phase 3 (edge cases, dry-run)

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 6c.1 | `cd "$DIR" && "$MASH" --dry-run move ghost.xyz to docs` | No source resolved; LLM produces best-effort `mv` using literal `./ghost.xyz`. | ⬜ |
| 6c.2 | `cd "$DIR" && "$MASH" --dry-run move overveiw.md to archive` | Fuzzy matches `./docs/overview.md`. `Run: mv ./docs/overview.md ./archive/`. | ⬜ |
| 6c.3 | `cd "$DIR" && "$MASH" --dry-run move the pdf to archive` | Generates `mv` referencing `./reports/summary.pdf` and `./archive/`. | ⬜ |
| 6c.4 | `cd "$DIR" && "$MASH" --dry-run move main.py to docs and rename it to app.py` | Single `mv <auto-selected main.py> ./docs/app.py`. | ⬜ |

### 6d — Phase 4 (flag behaviour)

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 6d.1 | `cd "$DIR" && "$MASH"` (no args) | Prints usage line, exit code 1. | ⬜ |
| 6d.2 | `cd "$DIR" && "$MASH" --dry-run list files in docs` | Output ends with `[dry-run] command not executed.` | ⬜ |
| 6d.3 | `cd "$DIR" && "$MASH" --yes create a file called flagtest.txt` | No `[y/N]` prompt; `flagtest.txt` exists. | ⬜ |

---

## Phase 8 — Verify 3.5 / 3.6 / 3.7 (rename extension, touch destination)

Regression for the smaller bug-fix plans that landed before the behaviour overhaul.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 8.1 (rename keeps ext when omitted) | `cd "$DIR" && printf '1\nn\n' \| "$MASH" rename overview to summary` | Source resolves to `./docs/overview.md`. Rename target becomes `./docs/summary.md` (extension auto-kept, no prompt). `[y/N]` answered `n`. | ⬜ |
| 8.2 (rename: different ext prompts) | `cd "$DIR" && printf '1\n1\nn\n' \| "$MASH" rename overview.md to summary.txt` | Prompt offers `keep '.md' / use '.txt' / cancel`. Input `1` selects keep → `Run: mv ./docs/overview.md ./docs/summary.md`. | ⬜ |
| 8.3 (rename: same ext skips prompt) | `cd "$DIR" && printf '1\nn\n' \| "$MASH" rename overview.md to readme.md` | No extension prompt. `Run: mv ./docs/overview.md ./docs/readme.md`. | ⬜ |
| 8.4 (rename: add ext to ext-less file) | `mkdir -p "$DIR/tmpfix" && touch "$DIR/tmpfix/notesfile" && cd "$DIR" && printf '1\n2\nn\n' \| "$MASH" rename notesfile to notesfile.md` then `rm -rf "$DIR/tmpfix"` | Add-extension prompt fires (keep none / add `.md` / cancel). Input `2` → `Run: mv ./tmpfix/notesfile ./tmpfix/notesfile.md`. | ⬜ |
| 8.5 (touch in destination directory) | `cd "$DIR" && "$MASH" --dry-run create a file called readme.md in docs` | `Run: touch ./docs/readme.md` — not `./readme.md`. | ⬜ |
| 8.6 (touch in non-existent destination → mkdir &&) | `cd "$DIR" && printf '2\n' \| "$MASH" --dry-run create a file called readme.md in newdir` | `Run: mkdir -p ./newdir && touch ./newdir/readme.md`. | ⬜ |

---

## Phase 9 — Verify 3.8 (universal menu template + abs paths + delete confirmation)

The disambiguation template is now unified: header → numbered options → blank line → footer `Select option [1-N], Enter to cancel, type name for other file/folder:`. Every path is rendered absolute. The current-directory entry carries the `(Current directory)` suffix. Single-match source/destination uses `[1]`, not yes/no. Delete shows an extra absolute-path confirmation.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 9.1 (multi source uses universal template + abs paths) | `cd "$DIR" && printf '\n' \| "$MASH" remove main` | Header `Mash found ... for 'main':`. Both candidates rendered as absolute paths (start with `/Users/`). Footer matches `Select option [1-2], Enter to cancel, type name for other file/folder:`. | ⬜ |
| 9.2 (single source still numbered) | `cd "$DIR" && printf '\n' \| "$MASH" delete overview` | Single-match menu prints `1. /Users/.../docs/overview.md` (NOT `Found: ./docs/overview.md`). Footer mentions `[1]`. | ⬜ |
| 9.3 (destination menu shows cwd labelled) | `cd "$DIR" && printf '\n' \| "$MASH" --dry-run move data.csv to reports` | Destination menu includes an entry suffixed `  (Current directory)` matching the abs cwd. | ⬜ |
| 9.4 (destination not-found options abs + cwd) | `cd "$DIR" && printf '\n' \| "$MASH" --dry-run move data.csv to nowheredir` | Options include `select <abs cwd>  (Current directory)` and `create <abs cwd>/nowheredir`. | ⬜ |
| 9.5 (delete extra confirmation block) | `cd "$DIR" && printf '1\nn\nn\n' \| "$MASH" delete overview` | Before the `Run:` line, mash prints `Are you sure you want to delete:`, blank line, indented absolute path, blank line, `[y/N] :`. First `n` cancels at the deletion confirmation (no `Run:` line printed). | ⬜ |
| 9.6 (delete confirm answered y proceeds to `Run:`) | `cd "$DIR" && printf '1\ny\nn\n' \| "$MASH" delete overview` | After `y` to the delete confirmation, `Run: rm ./docs/overview.md` appears; the trailing `n` declines execution. | ⬜ |
| 9.7 (`--yes` skips delete confirmation) | `cd "$DIR" && "$MASH" --dry-run delete overview` | No `Are you sure you want to delete:` block; only the standard `[dry-run] Auto-selected:` + `Run:` lines. | ⬜ |
| 9.8 (copy synonyms route to cp) | `cd "$DIR" && "$MASH" --dry-run duplicate config.json to archive` | `Run: cp ./config.json ./archive/`. | ⬜ |

---

## Phase 10 — Verify 3.9 list flow (basic two-stage menu)

Fixture must include at least one folder and several files in cwd. Reset before this phase.

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 10.1 (list cwd, folder annotation) | `cd "$DIR" && printf '\n' \| "$MASH" list files` | Header `Mash listing .:`. Each folder entry has `  (folder)` suffix. Final entry is `<abs cwd>  (Current directory)`. Blank cancels. | ⬜ |
| 10.2 (list → folder → action menu) | `cd "$DIR" && printf '1\n\n' \| "$MASH" list files` (assuming option 1 is a folder) | After selecting a folder, mash shows `Choose action for ...` menu containing in order: `go into (cd)`, `add (create file/folder inside)`, `rename`, `move`, `copy`, `remove`, `list contents`, `chmod`. Blank cancels. | ⬜ |
| 10.3 (list → file → action menu) | `cd "$DIR" && printf '<N>\n\n' \| "$MASH" list files` where `<N>` is a file row | Action menu lists `open`, `cat`, `rename`, `move`, `copy`, `remove`, `chmod`. No `go into` / `add` / `list contents` entries. | ⬜ |
| 10.4 (list → file → open routes to confirmation) | `cd "$DIR" && printf '<N>\n1\nn\n' \| "$MASH" list files` (N selects `config.json`, then action 1 = `open`) | Mash prints `Are you sure you want to open:` block with abs path. `n` declines. | ⬜ |
| 10.5 (list → file → remove routes to delete confirmation) | `cd "$DIR" && printf '<N>\n6\nn\n' \| "$MASH" list files` | `Are you sure you want to delete:` block fires with abs path. | ⬜ |
| 10.6 (list under `--dry-run`) | `cd "$DIR" && "$MASH" --dry-run list files` | Auto-selects entry 1 of the first menu; second (action) menu is cancelled. No `Run:` for destructive actions; no crash. | ⬜ |

---

## Phase 11 — Verify 3.9 open/cat + create-without-marker

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 11.1 (open existing: extra confirm block) | `cd "$DIR" && printf '1\nn\n' \| "$MASH" open overview` | Source menu fires; then `Are you sure you want to open:` block; `n` declines. No `Run:` line. | ⬜ |
| 11.2 (open existing: y runs) | `cd "$DIR" && printf '1\ny\nn\n' \| "$MASH" --dry-run open overview` | `Run: open ./docs/overview.md` printed; under `--dry-run`, not executed. | ⬜ |
| 11.3 (open not-found: search menu) | `cd "$DIR" && printf '2\n' \| "$MASH" open ghostxyz` | Header `Mash did not find a matching file, choose how to proceed:`; options `1. search for file`, `2. cancel`; `2` cancels. | ⬜ |
| 11.4 (open not-found: search → find) | `cd "$DIR" && printf '1\noverview\n1\nn\n' \| "$MASH" open ghostxyz` | `1` triggers search; typing `overview` finds the file; the open confirmation fires; `n` declines. | ⬜ |
| 11.5 (cat existing: confirm block) | `cd "$DIR" && printf '1\nn\n' \| "$MASH" cat overview` | `Are you sure you want to cat:` block with abs path; `n` declines. | ⬜ |
| 11.6 (cat not-found: same menu as open) | `cd "$DIR" && printf '2\n' \| "$MASH" cat ghostxyz` | Same `1. search for file / 2. cancel` menu as open. | ⬜ |
| 11.7 (create-without-marker BUG fix) | `cd "$DIR" && printf '1\nn\n' \| "$MASH" create testing.pdf` | Destination menu MUST fire (header `Where should the new file be created?` or equivalent), option 1 = `<abs cwd>  (Current directory)`. `n` declines. mash MUST NOT skip straight to `Run: touch ./testing.pdf`. | ⬜ |
| 11.8 (create with destination marker still works) | `cd "$DIR" && "$MASH" --dry-run create testing.pdf in docs` | `Run: touch ./docs/testing.pdf`. | ⬜ |

---

## Phase 12 — Verify 3.10 move/copy missing-dest + list-routed rename + list header/cwd

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 12.1 (move with no destination → fallback menu) | `cd "$DIR" && printf '1\n1\nn\n' \| "$MASH" move config.json` | After source pick, mash prints `Where should this be moved? Choose a destination:` with option `1. <abs cwd>  (Current directory)` and footer `Select option [1], Enter to cancel, type destination:`. `1` → `Run: mv ./config.json ./`. `n` declines. No `Mash did not find directory 'unknown'`. | ⬜ |
| 12.2 (copy with no destination → fallback menu, header says copied) | `cd "$DIR" && printf '1\n1\nn\n' \| "$MASH" copy config.json` | Same shape as 12.1 but header reads `Where should this be copied?`. | ⬜ |
| 12.3 (list-routed move uses fallback, not literal `unknown`) | `cd "$DIR" && printf '<N>\n4\n1\nn\n' \| "$MASH" list files` where N selects a file and 4 = `move` | The destination prompt is the no-destination fallback; NEVER `Mash did not find directory 'unknown'`. | ⬜ |
| 12.4 (list-routed rename keeps extension) | `cd "$DIR" && printf '<N>\n3\napple\nn\n' \| "$MASH" list files` where N selects `config.json` (or any file with extension) and 3 = `rename` | Result is `Run: mv ./config.json ./apple.json` (extension auto-kept). `n` declines. Must NOT produce `mv ./config.json ./apple` without extension. | ⬜ |
| 12.5 (list-routed rename: different ext still prompts) | `cd "$DIR" && printf '<N>\n3\napple.txt\n1\nn\n' \| "$MASH" list files` where N selects `config.json` | Different-extension prompt fires (`keep '.json' / use '.txt' / cancel`); input `1` → `Run: mv ./config.json ./apple.json`. | ⬜ |
| 12.6 (list header relative path, cwd as last entry) | `cd "$DIR" && printf '\n' \| "$MASH" list files` | Header text is exactly `Mash listing .:` (not absolute). Last numbered entry is the cwd labelled `(Current directory)`. | ⬜ |
| 12.7 (list subdir header relative) | `cd "$DIR" && printf '<F>\n1\n\n' \| "$MASH" list files` where `<F>` selects the `docs` folder and `1 = go into (cd)` | After drilling in, header is `Mash listing ./docs:`. Final entry is the abs path of `docs` with `(Current directory)` suffix. | ⬜ |
| 12.8 (cwd entry → folder action menu) | `cd "$DIR" && printf '<LAST>\n\n' \| "$MASH" list files` where `<LAST>` selects the final `(Current directory)` entry | Action menu shown is the FOLDER set (`go into`, `add`, `rename`, `move`, `copy`, `remove`, `list contents`, `chmod`). | ⬜ |

---

## Phase 13 — Verify 3.10 list typed-prompt + bare-name extension + file/folder disambig

| # | Command | Pass criteria | Status |
|---|---------|---------------|--------|
| 13.1 (typed folder name switches scope) | `cd "$DIR" && printf 'docs\n\n' \| "$MASH" list files` | Typing `docs` re-scopes the listing to `./docs`: header becomes `Mash listing ./docs:`. Blank cancels. | ⬜ |
| 13.2 (typed file name narrows listing) | `cd "$DIR" && printf 'overview\n\n' \| "$MASH" list files in docs` | Listing narrows to just `./docs/overview.md` (single file plus `(Current directory)` entry). | ⬜ |
| 13.3 (typed name with no match: notice + re-render) | `cd "$DIR" && printf 'ghostxyz\n\n' \| "$MASH" list files` | Output shows `Mash could not find 'ghostxyz' in .` (or equivalent) and re-renders the original listing. Blank cancels. | ⬜ |
| 13.4 (typed name does NOT exit list flow) | `cd "$DIR" && printf 'docs\n\n' \| "$MASH" list files` | Mash must NOT re-shell into a new `mash docs` invocation; it must scope within list flow. (Check there's no second `Run:` line and no LLM call output.) | ⬜ |
| 13.5 (`mash list <dir>` opens that dir directly) | `cd "$DIR" && printf '\n' \| "$MASH" list docs` | Listing starts already scoped to `./docs` (header `Mash listing ./docs:`). | ⬜ |
| 13.6 (bare create disambiguates file/folder) | `cd "$DIR" && printf '2\n1\nn\n' \| "$MASH" create drafts` | Mash prints `Mash is not sure what you want to create, choose:` with options `1. create file`, `2. create folder`. Picking `2` routes into folder-create, asks destination (option 1 = cwd), `n` declines. | ⬜ |
| 13.7 (bare create → file routes to extension picker) | `cd "$DIR" && printf '1\napple\n1\n1\nn\n' \| "$MASH" create drafts` | After picking `1` (file), name prompt or destination flow runs; once name is `apple` with no extension, the extension picker MUST appear before the `Run:` line. | ⬜ |
| 13.8 (list `add` → file → bare name triggers extension picker) | `cd "$DIR" && printf '<F>\n2\n1\napple\n1\nn\n' \| "$MASH" list files` where F selects a folder and 2 = `add`, then 1 = `create file` | Mash prompts for extension when name `apple` has no dot. Final `Run:` is `touch <chosen folder>/apple.<ext>`, not `touch <folder>/apple`. | ⬜ |
| 13.9 (`--yes` extension default = first tree ext) | `cd "$DIR" && "$MASH" --yes create foo` (after fixture reset; tree has `.md`, `.txt`, `.pdf`, `.py`, `.json`, `.csv`) | Under `--yes`, mash assumes file (or whichever is the configured default for bare create), auto-picks the most common tree extension, and runs `touch ./foo.<ext>`. No interactive prompt. Cleanup any created file. | ⬜ |
| 13.10 (`--yes` extension fallback `.txt` when tree empty) | `mkdir -p "$DIR/empty" && cd "$DIR/empty" && "$MASH" --yes create foo && test -f "$DIR/empty/foo.txt" && rm -rf "$DIR/empty"` | Under empty tree, `--yes` falls back to `.txt`. `foo.txt` exists. | ⬜ |

---

## Phase 14 — Final cleanup and report

| # | Command | Status |
|---|---------|--------|
| 14.1 | `cd "$DIR" && find . -mindepth 1 -not -path "*/.*" -not -name "bugs_found.md" -delete` (clean slate; preserve `bugs_found.md`) | ⬜ |
| 14.2 | **Update** `Claude_testing/bugs_found.md` in-place with the results of this run: append new bug entries for any newly observed failures (do not duplicate existing entries), mark previously listed bugs as resolved if their test now passes, and refresh the summary table at the bottom of the file with per-test PASS/FAIL. Never overwrite or delete prior bug entries — only append, edit status, or strike through. | ⬜ |

For each FAIL, capture inside the appended/edited entry:
- Exact invocation
- Full stdout/stderr
- Expected behaviour
- Suspected root cause (one sentence)

**`bugs_found.md` protection rules:**
- The file MUST exist before and after every phase. If missing, stop and report — do not recreate it from scratch.
- Phase 0 and Phase 6b fixture resets explicitly exclude `bugs_found.md` from deletion (see the `-not -name "bugs_found.md"` predicate).
- Never run `rm` or `find -delete` against the file. Updates happen via Edit/Write, never replacement of the whole file unless every prior bug entry is preserved verbatim.

---

## Claude Instructions

- **Scope:** Every filesystem mutation must target a path under `/Users/max/Documents/GitHub/Mash agent/Claude_testing/`. Never read, write, or delete anywhere else. The only file outside the source tree you may write is the test report — which lives inside `Claude_testing/`.
- **Read-only files outside scope:** You MAY read `mash/*.py` source files for diagnostic context when a test fails, but never edit them.
- **Interactive menus:** Pipe input via `printf '...\n' | "$MASH" ...`. For multi-step flows (retype → re-resolve → pick → y/N) chain inputs in order separated by `\n`. The last `n` or `N` typically declines the `[y/N]` execute prompt so no real mutation happens.
- **Capture output:** For every test, capture both stdout and stderr (`2>&1`). Save the full output of any failing test verbatim in the report.
- **Order:** Run phases in numeric order. Reset fixtures (Phase 0) before Phase 1, again before Phase 6b, and again before Phases 8 / 10 / 12 / 13 since several earlier phases mutate the tree. Phase 4.3 deletes `./main.py`; restore it before continuing.
- **Interactive index placeholders:** several new tests use `<N>`, `<F>`, `<LAST>` to denote "the menu index of file/folder X in the current run". The agent must first run the listing once (or look at the fixture) to determine the correct index, then re-run with the chosen number piped in. Indices are 1-based and reflect the order mash prints — folders may appear before files in the menu; the `(Current directory)` entry is always last.
- **Stop conditions:** If a fixture-reset command fails, stop and report — do not continue with a dirty tree. If `$MASH` is missing, stop.
- **Do not fix bugs.** Document them only. Bug fixes are tracked in their own implementation plans.
- **Final deliverable:** updated `Claude_testing/bugs_found.md` reflecting this run's findings (append-only for new bugs; status updates for fixed ones). Phase 14.1 cleanup is mandatory but MUST NOT delete `bugs_found.md`.
- **Plan 3.5 contract:** Once `implementation_plans/3.5_bug_fix.md` lands, `--dry-run` and `--yes` intentionally bypass all interactive menus (see Phase 7 of plan 3.5). Tests 2.1, 2.2, 5b.1–5b.3, and 5c.1–5c.9 — which feed numeric/manual input via `printf` under `--dry-run` — are expected to auto-select rather than honour the piped input. Mark these PASS if the auto-select line matches the Phase 7 contract; flag as a regression only if the output diverges from that contract.

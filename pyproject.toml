[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mash-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["ollama"]

[tool.setuptools.packages.find]
include = ["mash*"]

[project.scripts]
mash = "mash.main:run"
